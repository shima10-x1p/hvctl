# Project Instructions

## コマンド
- `dotnet build` : ビルドを実行する。
- `dotnet test` : テストを実行する。
- `dotnet format --verify-no-changes` : コードフォーマットを検証する。

## コーディングルール
- nullable reference typesを有効にする。
- 基本データ型より、Domain型を優先する。
- 型システムによって非nullが保証される場合は、防御的なnullチェックを行わない。
- 不要な防御的プログラミングを行わない。後方互換性を維持する必要もない。与えられた仕様に従って、必要なコードのみを実装する。

## ドキュメンテーション
- クラス・メソッド・プロパティには、XMLドキュメントコメントを付与する。
- コードの意図を説明する場合や、複雑なロジックを説明する場合は、XMLドキュメントコメントではなく、コード内にコメントを記述する。
- 単純な翻訳になるようなコメントは避ける。コードの意図や、なぜそのように実装したのかを説明するコメントを記述する。

## Response Policy

- 回答は原則として簡潔にする。
- 実装時は、変更方針・変更ファイル・検証コマンドだけを出す。
- ファイル全文は要求された場合だけ出す。
- 差分説明は要点のみ。詳細な解説、背景説明、代替案の列挙は不要。
- 迷った場合は、長い説明ではなく確認事項を最大3つまで出す。
- 変更後は、実行したテスト結果または未実行理由だけを書く。